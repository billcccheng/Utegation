<mvc:View xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" controllerName="sap.ui.utegation.controller.Preference">
    <Panel headerText="{i18n>GasHeader}" class="sapUiResponsiveMargin" width="auto">
      <content>
        <Label text="PaperLess" class="sapUiSmallMargin" />
        <Text text = "{title}"/>
      </content>
        <Switch id="showHideHeaderSwitch"
    		state="true"
    		class="sapUiSmallMarginBottom"
    		change="onHeaderSwitchChange">
        </Switch>
    </Panel>
</mvc:View>